var assert = chai.assert, _ = Underscore();

describe('one', function () {

    it('should say yes', function () {
        var x = 42;
        if (_.isUndefined(x)) {
        }

        assert.equal(a(), 42, 'caramelo');
    });
});

